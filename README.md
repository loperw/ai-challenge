# AI Chat — интерфейс для нескольких AI-провайдеров

Небольшое локальное веб-приложение для общения с моделями DeepSeek, OpenRouter и Gemini через API. Оно состоит из чата, изолированного серверного агента и правой панели настроек, где можно выбрать используемую модель и температуру ответа.

## Возможности

- Отправка сообщений в DeepSeek V4.1 Flash, OpenRouter и Gemini API.
- Потоковая выдача: текст ответа появляется по мере генерации.
- Модели разделены в списке на группы DeepSeek, OpenRouter и Gemini 3.5.
- Класс `Agent` хранит контекст диалога, применяет системный промпт и параметры генерации, вызывает API и разбирает потоковый ответ.
- Каждый чат получает отдельный экземпляр агента с изолированным стеком сообщений (в памяти сервера).
- Настройка температуры: от более точных до более творческих ответов.
- API-ключ не отображается в браузере и не передаётся клиентскому коду.

## Ветки разработки

| Ветка | Содержание |
| --- | --- |
| `main` | Исходный интерфейс AI-чата. |
| `codex/ai-challenge-2` | Управление форматом ответа, лимитом токенов и stop sequence. |
| `codex/ai-challenge-3` | История чатов и сравнение ответов. |
| `codex/ai-challenge-4` | Настройка температуры ответов. |
| `codex/ai-challenge-5` | DeepSeek, OpenRouter и Gemini; сохранение параметров для каждого чата и сравнение с учётом модели и температуры. |
| `codex/ai-challenge-6` | Изолированный серверный агент со своим стеком сообщений; DeepSeek V4.1 Flash. |

## Запуск локально

1. Создайте файл `.env` рядом с `server.js`, скопировав содержимое `.env.example`.
2. Укажите ключи тех сервисов, которыми планируете пользоваться:

   ```env
   DEEPSEEK_API_KEY=ваш_ключ_DeepSeek
   OPENROUTER_API_KEY=ваш_ключ_OpenRouter
   GEMINI_API_KEY=ваш_ключ_Gemini
   AGENT_SYSTEM_PROMPT=необязательный системный промпт агента
   ```

3. Запустите сервер:

   ```bash
   node server.js
   ```

4. Откройте [http://localhost:3000](http://localhost:3000).

Открывать `public/index.html` напрямую как локальный файл не нужно: запросы чата обслуживает Node.js-сервер. Если интерфейс сообщает, что сервер недоступен, снова выполните `node server.js` и обновите страницу по адресу выше.

## Безопасность ключа

Файл `.env` включён в `.gitignore`, поэтому не будет добавлен в Git и опубликован на GitHub. В репозитории остаётся только `.env.example` без настоящего ключа.

При публикации приложения на сервере добавьте нужные переменные окружения (`DEEPSEEK_API_KEY`, `OPENROUTER_API_KEY`, `GEMINI_API_KEY`) в настройках хостинга, а не в исходный код.

`AGENT_SYSTEM_PROMPT` задаёт необязательную общую системную инструкцию. Температура, лимит токенов, stop sequence и JSON-режим передаются из интерфейса, но применяются внутри агента. История агента хранится в памяти процесса и очищается при перезапуске сервера.
